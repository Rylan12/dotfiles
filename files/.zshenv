# This file is always loaded for zsh.
# It should only contain configuration that should be used when programs use zsh (i.e. scripts)

# Set XDG config directory
export XDG_CONFIG_HOME="$HOME/.config"

export EDITOR="nvim"
export LESS="-FIR --mouse"
export PAGER="less"
